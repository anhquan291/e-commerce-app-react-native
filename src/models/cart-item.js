class CartItem {
  constructor(id, quantity, price, name, sum, imageUrl) {
    this.id = id;
    this.quantity = quantity;
    this.price = price;
    this.name = name;
    this.sum = sum;
    this.imageUrl = imageUrl;
  }
}

export default CartItem;
